name: Package

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    name: Package (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - name: linux-x64
            os: ubuntu-24.04
            enable_android_artifacts: true
          - name: linux-arm64
            os: ubuntu-24.04-arm
            enable_android_artifacts: false
          - name: windows-x64
            os: windows-2022
            enable_android_artifacts: false
          - name: macos-arm64
            os: macos-15
            enable_android_artifacts: false

    env:
      KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
      KEY_CONTENT: ${{ secrets.KEY_CONTENT }}
      KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install Linux packaging tools
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y fakeroot rpm

      - name: Make Gradle executable
        if: runner.os != 'Windows'
        run: chmod +x ./gradlew

      - name: Package distributions
        run: ./gradlew packageDistributions -PenableAndroidArtifacts=${{ matrix.enable_android_artifacts }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dormnet-${{ matrix.name }}
          path: build/ci-artifacts/**
          if-no-files-found: error
